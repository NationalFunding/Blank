<?xml version="1.0" encoding="utf-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Statements_In_Date_Must_Exist</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    $Setup.ValidationRules__c.Bypass_Opportunity_Stage_Validations__c = false,
    OR(
        RecordType.Name = 'Broker WC Oppty',
        RecordType.Name = 'Working Capital Oppty'), 
        ISBLANK(Statements_In_Date__c),
    OR(
        ISPICKVAL(StageName, 'Pre-Qual MCA'),
        ISPICKVAL(StageName, 'Approved - MCA'),
        ISPICKVAL(StageName, 'Internal Decline'),
        ISPICKVAL(StageName, 'Declined'),
        ISPICKVAL(StageName, 'NF Decline')
    )
)</errorConditionFormula>
    <errorMessage>Prior to the Stage that was selected, this opportunity must be enter the Statements In stage.</errorMessage>
</ValidationRule>
