<?xml version="1.0" encoding="utf-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Docs_Out_Info_Incomplete_External</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    OR(
        ISBLANK(Amount_Collected__c),
        ISBLANK(Amount_Offered__c),
        ISBLANK(Docs_Term__c),
        ISBLANK(WC_Buy_Rate__c),
        ISBLANK(Ratio__c)
    ),
    OR(
        RecordType.Name = 'Working Capital Oppty',
        RecordType.Name = 'FRPSA'
    ),
    OR(
        NOT(CONTAINS(Lender_Account__r.Name, 'National Funding')),
        ISBLANK(Lender_Account__c)
    ),
    NOT(ISPICKVAL(Docs_Payment_Frequency__c, 'Monthly')),
    ISCHANGED(StageName),
    ISPICKVAL(StageName, 'Submission - MCA/ALT')
)</errorConditionFormula>
    <errorMessage>The Amount Collected, Amount Doc'd, Docs Term, WC Buy Rate, and WC Sell Rate must all be populated before moving this deal to the 'Submission' Stage.</errorMessage>
</ValidationRule>
