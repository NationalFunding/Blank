<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VAP_Count__c</fullName>
    <description>This field will show a 1 if the vendor is part of the VAP program and 0 if not.  This is for summary reporting.</description>
    <externalId>false</externalId>
    <formula>IF( ISPICKVAL(Type, "Vendor - VAP") , 1, 0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This field will show a 1 if the vendor is part of the VAP program and 0 if not.  This is for summary reporting.</inlineHelpText>
    <label>VAP Count</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
