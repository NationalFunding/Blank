<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Process_On__c</fullName>
    <defaultValue>TODAY()</defaultValue>
    <description>Date when the Queued Event should be processed if it's in the future, if empty, the Queued Event will be processed next run.</description>
    <inlineHelpText>Date when the Queued Event should be processed if it's in the future, if empty, the Queued Event will be processed next run.</inlineHelpText>
    <label>Process On</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
